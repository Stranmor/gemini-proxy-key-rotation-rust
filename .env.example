# =============================================================================
# Docker Environment - Docker Compose only
# Copy this file to .env and customize for your needs
# =============================================================================

# External Redis port (change if 6379 is occupied on host)
REDIS_PORT=6379

# Docker container logging
RUST_LOG=info                      # debug, info, warn, error
RUST_BACKTRACE=0                   # 1 for debugging

# Redis UI (tools profile)
REDIS_UI_PORT=8082
REDIS_UI_USER=admin
REDIS_UI_PASSWORD=change_me_please

# =============================================================================
# ALL application settings are in config.yaml:
# - Proxy port (server.port) - default 4806 in docker-compose (mapped), override via PORT env
# - Gemini API keys and groups
# - Redis URL and settings
# - Timeouts, limits, circuit breaker
# =============================================================================